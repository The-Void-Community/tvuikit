import { Wrapper } from "../wrapper";

import { Button } from "./button";
import { Text } from "./text";

export const Page = () => {
  return (
    <Wrapper>
      <Text />
      <Button />
    </Wrapper>
  );
};
